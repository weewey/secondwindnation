apiVersion: v1
kind: Pod
metadata:
  name: frontend
  labels:
    app: frontend
spec:
  containers:
   - name: frontend
     image: gcr.io/swn-jenkins/frontend:v1
     ports:
     - name: http
       containerPort: 8081
